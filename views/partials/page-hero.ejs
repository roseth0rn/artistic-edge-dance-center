<section class="page-hero">
  <div class="wrap">
    <% if (typeof eyebrow !== 'undefined' && eyebrow) { %><p class="eyebrow"><%= eyebrow %></p><% } %>
    <h1><%- heroTitle %></h1>
    <% if (typeof lede !== 'undefined' && lede) { %><p class="muted"><%= lede %></p><% } %>
  </div>
</section>
